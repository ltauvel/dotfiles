#!/bin/bash

# MODE: all
# PLATEFORM: all
# PRIORITY:

# Creating symbolic links to global shell conf
ln -sf $HOME/.dotfiles/shell/aliases.sh $HOME/.aliases
ln -sf $HOME/.dotfiles/shell/functions.sh $HOME/.functions